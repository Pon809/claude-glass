{
  "name": "claude-glass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "control-ui (Lauren Tan / Cursor) ported to Claude Code, with a FEATURE_MAP template and a demo app.",
  "scripts": {
    "serve": "serve demo -l 3000",
    "probe": "node demo/harness/probe.mjs 3000"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "serve": "^14.2.0"
  }
}
